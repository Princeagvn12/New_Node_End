MONGO_URI=
PORT=4000
JWT_ACCESS_TOKEN_SECRET=
JWT_REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

# Cookie settings
# Dev (same-site): COOKIE_SECURE=false, COOKIE_SAME_SITE=lax
# Cross-site HTTPS (frontend/backend on different domains): COOKIE_SECURE=true, COOKIE_SAME_SITE=none
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax

# Comma-separated list of allowed frontend origins
CLIENT_ORIGIN=http://localhost:5173,https://gestion-learn.vercel.app

# Example Mongo URI (do not commit real secrets)
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/<db>
