MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/<database>?retryWrites=true&w=majority
PORT=4000
JWT_ACCESS_TOKEN_SECRET=replace_with_a_long_random_secret
JWT_REFRESH_TOKEN_SECRET=replace_with_a_different_long_random_secret
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
# Set to true in production when serving over HTTPS
COOKIE_SECURE=false
CLIENT_ORIGIN=http://localhost:5173

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-app-password
MAIL_FROM="Gestion RH <no-reply@example.com>"

FRONTEND_URL=http://localhost:5173
# RESET_TOKEN_TTL_MS=3600000
