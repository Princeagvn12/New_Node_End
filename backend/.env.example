MONGO_URI=
PORT=4000
JWT_ACCESS_TOKEN_SECRET=
JWT_REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
COOKIE_SECURE=false  # true en prod
CLIENT_ORIGIN=http://localhost:5173

# Example of a MongoDB connection string (put the real value in backend/.env, do NOT commit secrets):
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.edx03fy.mongodb.net/gestion_db